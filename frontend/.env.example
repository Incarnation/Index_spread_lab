### Frontend runtime settings (Vite only reads VITE_* keys)
# For local dev with Vite proxy, leave empty.
# For production or split-origin deployment, set to the full API origin (no trailing slash).
VITE_API_BASE_URL=

### Optional: enables admin quick actions in the dashboard UI
VITE_ADMIN_API_KEY=
